package me.colingrimes.packets;

import me.colingrimes.midnight.Midnight;

/**
 * Random Notes:
 * - ProtocolLib sometimes uses different types compared to the Protocol website.
 * - /packet add play server [packet_name] true -> Pretty helpful to debug field types for packets.
 * - Or it usually has its own structure with a similar name to the packet.
 */
public class Packets extends Midnight {

	@Override
	protected void enable() {
		// Auto-generated method stub.
	}
}
